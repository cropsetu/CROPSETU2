{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "if [ \"$ROLE\" = \"worker\" ]; then celery -A jobs.queue:celery_app worker --loglevel=info --concurrency=${CELERY_CONCURRENCY:-2}; else uvicorn main:app --host 0.0.0.0 --port $PORT --workers ${WEB_CONCURRENCY:-2} --no-access-log; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
